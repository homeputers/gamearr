{
  "name": "@gamearr/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "echo dev domain",
    "lint": "echo lint domain",
    "test": "tsc -p tsconfig.json && node --test dist/**/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}
